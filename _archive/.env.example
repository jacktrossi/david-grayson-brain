# These are the ONLY two values that live in .env
# Everything else (Anthropic key, Gmail tokens, etc.) is stored
# encrypted in Supabase Vault and loaded automatically at startup.

SUPABASE_URL=https://geukgwzmygiyvgitemoy.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
